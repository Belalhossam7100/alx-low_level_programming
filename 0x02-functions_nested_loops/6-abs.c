#include "main.h"
/**
 *abs - makes the absloute value of an integer
 *@i;is an integer
 * Return: if i is postive i  if negative -i
 */
int _abs(int i)
{
if (i >= 0)
{
return (i);
}
else
{
return (-i);
}
}
